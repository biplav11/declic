{
  "name": "@declic/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "./pocketbase serve",
    "serve": "./pocketbase serve",
    "build": "echo 'no build step for pocketbase' && exit 0",
    "lint": "echo 'no lint configured' && exit 0",
    "setup": "bash scripts/setup.sh"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "ioredis": "^5.3.2",
    "pocketbase": "^0.21.0"
  }
}
